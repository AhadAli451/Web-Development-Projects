```javascript
        function nameGenerator() {
    let names = [
        "Crazy Foods Limited",
        "Amazing Engine Hub",
        "Fire Garments Bors"
    ]; // Use an array for multiple names


    return names[Math.floor(Math.random() * names.length)]; // Pick a random name
    for (let i = 0; let < 3; i++) {
}}

console.log(nameGenerator());
```